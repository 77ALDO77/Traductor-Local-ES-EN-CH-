services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: boc_translator_backend_v1
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - MODEL_NAME=qwen2.5:3b
      - CLEANUP_INTERVAL_MINUTES=60
      - FILE_RETENTION_MINUTES=1440
      - LOG_LEVEL=INFO
      - ADMIN_USERNAME=admin_boc
      - ADMIN_PASSWORD=seguridad_boc_2026
      - TZ=America/Lima
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    volumes:
      - boc_whisper_data:/app/models
      - boc_uploads:/app/uploads
      - ./static:/app/static
      - ./templates:/app/templates
      - ./backend:/app/backend
    networks:
      - translator_network
    depends_on:
      - ollama
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  nginx:
    image: nginx:alpine
    container_name: boc_translator_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - backend
    networks:
      - translator_network

  ollama:
    image: ollama/ollama:latest
    container_name: boc_translator_ollama_v1
    restart: unless-stopped
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_GPU=1
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - OLLAMA_FLASH_ATTENTION=true
      - OLLAMA_KEEP_ALIVE=5m
      - OLLAMA_NUM_PARALLEL=4
      - OLLAMA_MAX_LOADED_MODELS=1
      - TZ=America/Lima
    volumes:
      - boc_ollama_data:/root/.ollama
    networks:
      - translator_network
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]

  translator_engine:
    build:
      context: ./translator_engine
    container_name: boc_translator_engine_v1
    restart: always
    ports:
      - "9000:9000"
    volumes:
      - boc_whisper_data:/app/models_whisper
      - ./translator_engine:/app
    environment:
      - WHISPER_MODEL_SIZE=medium
      - WHISPER_DEVICE=cuda
      - WHISPER_COMPUTE_TYPE=int8_float16
      - NMT_DEVICE=cuda
      - NMT_COMPUTE_TYPE=float16
      - MAX_BATCH_SIZE=32
      - TZ=America/Lima
    networks:
      - translator_network
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]

networks:
  translator_network:
    driver: bridge
    name: boc_translator_net

volumes:
  boc_ollama_data:
    driver: local
    name: boc_ollama_data
  boc_whisper_data:
    driver: local
    name: boc_whisper_data
  boc_uploads:
    driver: local
    name: boc_uploads
